<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0-windows10.0.19041.0</TargetFramework>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <SupportedOSPlatformVersion>10.0.19041.0</SupportedOSPlatformVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <AssemblyName>TaskbarInfo</AssemblyName>
    <Product>TaskbarInfo</Product>
    <Title>TaskbarInfo</Title>
    <Version>1.1.3</Version>
    <AssemblyVersion>1.1.3.0</AssemblyVersion>
    <FileVersion>1.1.3.0</FileVersion>
    <InformationalVersion>1.1.3</InformationalVersion>
    <RepositoryUrl>https://github.com/yixing233/TaskbarInfo</RepositoryUrl>
    <ApplicationIcon>src\icons\LyricsX.ico</ApplicationIcon>
    <DefaultItemExcludes>$(DefaultItemExcludes);artifacts\**;bin\**;obj\**;publish\**;decompiler\**;installer\**;tests\**</DefaultItemExcludes>
  </PropertyGroup>
  
  <ItemGroup>
    <None Include="src\icons\LyricsX.ico" />
  </ItemGroup>

  <ItemGroup>
    <!-- Keep the standalone WinUI settings host out of the WPF project's default globs. -->
    <ApplicationDefinition Remove="Legacy\**\*.xaml;SettingsHost\**\*.xaml;Views\**\*.xaml" />
    <Page Remove="Legacy\**\*.xaml;SettingsHost\**\*.xaml;Views\**\*.xaml" />
    <Compile Remove="Legacy\**\*.cs;Hosting\**\*.cs;Interop\**\*.cs;Properties\**\*.cs;Services\**\*.cs;SettingsHost\**\*.cs;ViewModels\**\*.cs;Views\**\*.cs" />
  </ItemGroup>

  <Target Name="BuildAndCopySettingsHost" BeforeTargets="Build;CopyFilesToOutputDirectory">
    <MSBuild Projects="SettingsHost\LyricsX.Settings.csproj"
             Targets="Rebuild"
             Properties="Configuration=$(Configuration)" />
    <RemoveDir Directories="$(OutDir)SettingsHost" />
    <ItemGroup>
      <SettingsHostOutput Include="SettingsHost\$(BaseOutputPath)$(Configuration)\net9.0-windows10.0.19041.0\win-x64\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(SettingsHostOutput)"
          DestinationFiles="@(SettingsHostOutput->'$(OutDir)SettingsHost\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>

  <Target Name="CopySettingsHostToPublish" AfterTargets="Publish">
    <RemoveDir Directories="$(PublishDir)SettingsHost" />
    <ItemGroup>
      <SettingsHostPublishOutput Include="$(OutDir)SettingsHost\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(SettingsHostPublishOutput)"
          DestinationFiles="@(SettingsHostPublishOutput->'$(PublishDir)SettingsHost\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>

  <ItemGroup>
    <Resource Include="src\icons\*.png" />
    <Resource Include="Fonts\fa-solid-900.ttf" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="3v.EvtSource" Version="2.0.0" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.1" />
    <PackageReference Include="Dubya.WindowsMediaController" Version="2.5.6" />
    <PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />
    <PackageReference Include="Lyricify.Lyrics.Helper" Version="0.2.0" />
  </ItemGroup>

</Project>
