<Window x:Class="TaskbarInfo.TaskbarPerformanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LyricsX 性能监控"
        Width="180"
        Height="30"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="False"
        UseLayoutRounding="True"
        FontFamily="Microsoft YaHei UI, Segoe UI, sans-serif">
    <Window.Resources>
        <FontFamily x:Key="FontAwesome">./Fonts/fa-solid-900.ttf#Font Awesome 6 Free Solid</FontFamily>

        <!-- Keep the independent component menu visually identical to the lyric menu. -->
        <Style TargetType="ContextMenu">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Grid.IsSharedSizeScope" Value="True" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI, sans-serif"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Name="Border" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="0,4">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="16" Direction="270" Color="Black" Opacity="0.12" ShadowDepth="4" />
                            </Border.Effect>
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="Transparent" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="15"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,10,0"/>
                                <TextBlock x:Name="CheckIcon" Grid.Column="2" Text="&#xf00c;" FontFamily="{StaticResource FontAwesome}" FontSize="12" Foreground="#0067C0" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F2F2F2"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F0F7FF"/>
                                <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="#E8E8E8" SnapsToDevicePixels="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="设置" Click="OpenSettings_Click">
                <MenuItem.Icon><TextBlock Text="&#xf013;" FontFamily="{StaticResource FontAwesome}" FontSize="14" /></MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="检查更新..." Click="CheckForUpdates_Click">
                <MenuItem.Icon><TextBlock Text="&#xf0ab;" FontFamily="{StaticResource FontAwesome}" FontSize="12" /></MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="重启" Click="Restart_Click">
                <MenuItem.Icon><TextBlock Text="&#xf021;" FontFamily="{StaticResource FontAwesome}" FontSize="13" /></MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="退出" Click="Exit_Click">
                <MenuItem.Icon><TextBlock Text="&#xf00d;" FontFamily="{StaticResource FontAwesome}" FontSize="14" Foreground="#FF4D4F" /></MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </Window.ContextMenu>
    <Border x:Name="PerformanceBackground"
            Background="#33000000"
            CornerRadius="5"
            Padding="5,0"
            ClipToBounds="True"
            Cursor="SizeAll"
            ToolTip="拖动调整性能组件位置"
            MouseLeftButtonDown="DragHandle_MouseLeftButtonDown"
            MouseMove="DragHandle_MouseMove"
            MouseLeftButtonUp="DragHandle_MouseLeftButtonUp">
        <Grid x:Name="PerformanceGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="PerformanceText"
                       Margin="18,0,0,0"
                       Foreground="#D8FFFFFF"
                       FontSize="10"
                       VerticalAlignment="Center"
                       Grid.Row="0"
                       TextWrapping="NoWrap"
                       TextTrimming="CharacterEllipsis"
                       IsHitTestVisible="False" />
            <Border x:Name="DragHandle"
                    Width="12"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Stretch"
                    Grid.RowSpan="2"
                    Background="Transparent"
                    IsHitTestVisible="False">
                <Border x:Name="DragIndicator"
                        Width="3"
                        Height="12"
                        CornerRadius="2"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="#55FFFFFF" />
            </Border>
            <TextBlock x:Name="PerformanceTextSecond"
                       Margin="18,0,0,0"
                       Foreground="#D8FFFFFF"
                       FontSize="10"
                       VerticalAlignment="Center"
                       Grid.Row="1"
                       Visibility="Collapsed"
                       TextWrapping="NoWrap"
                       TextTrimming="CharacterEllipsis"
                       IsHitTestVisible="False" />
        </Grid>
    </Border>
</Window>
