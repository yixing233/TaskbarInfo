<Window x:Class="TaskbarInfo.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="颜色选择" Height="400" Width="360"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#F5F7FA" FontFamily="Microsoft YaHei UI, Segoe UI, sans-serif">
    <!-- Increased Width to 360 to accommodate sliders better -->
    
    <Window.Resources>
        <!-- Reusing resources from SettingsWindow for consistency -->
        <SolidColorBrush x:Key="ControlBorder" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#202020"/>
        <SolidColorBrush x:Key="SystemAccentColor" Color="#0067C0"/>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FBFBFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource SystemAccentColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource SystemAccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="White"/>
                        </Border>
                         <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1975C5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#1975C5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Style for Color Presets to avoid white hover background -->
        <Style x:Key="ColorSwatchButtonStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- Use TemplateBinding Background to keep the color -->
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{StaticResource ControlBorder}" 
                                BorderThickness="1" 
                                CornerRadius="4">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- On hover, just darken/thicken the border, do NOT change Background -->
                                <Setter TargetName="border" Property="BorderBrush" Value="#666666"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Slider Styles Copied from SettingsWindow -->
        <Style x:Key="SliderThumbStyle" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Grid>
                            <Ellipse x:Name="Ellipse" Fill="#FFFFFF" Stroke="{StaticResource ControlBorder}" StrokeThickness="1">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="Black" Opacity="0.12" BlurRadius="4" ShadowDepth="1"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <Ellipse x:Name="Inner" Fill="{StaticResource SystemAccentColor}" Width="8" Height="8" Opacity="0.8"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Ellipse" Property="Fill" Value="#FAFAFA"/>
                                <Setter TargetName="Inner" Property="Opacity" Value="1.0"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="Ellipse" Property="Fill" Value="#F0F0F0"/>
                                <Setter TargetName="Inner" Property="Width" Value="10"/>
                                <Setter TargetName="Inner" Property="Height" Value="10"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsSnapToTickEnabled" Value="True"/>
            <Setter Property="TickFrequency" Value="1"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Track Background -->
                            <Border Grid.Row="1" Height="4" Background="#E0E0E0" CornerRadius="2" VerticalAlignment="Center"/>
                            
                            <!-- Track -->
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Style>
                                            <Style TargetType="RepeatButton">
                                                <Setter Property="OverridesDefaultStyle" Value="True"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="IsTabStop" Value="False"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="RepeatButton">
                                                            <Border Height="4" Background="{StaticResource SystemAccentColor}" CornerRadius="2" VerticalAlignment="Center"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </RepeatButton.Style>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                        <RepeatButton.Style>
                                            <Style TargetType="RepeatButton">
                                                <Setter Property="OverridesDefaultStyle" Value="True"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="IsTabStop" Value="False"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="RepeatButton">
                                                            <Border Background="Transparent"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </RepeatButton.Style>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SliderThumbStyle}" VerticalAlignment="Center"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Preview -->
            <RowDefinition Height="Auto"/> <!-- Sliders -->
            <RowDefinition Height="*"/>    <!-- Spacer -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Preview and Hex -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Height="40" Width="40" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Background="White">
                <Rectangle x:Name="ColorPreview" RadiusX="6" RadiusY="6" Margin="4">
                   <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding StartColor}"/>
                   </Rectangle.Fill>
                </Rectangle>
            </Border>
            
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock Text="HEX Color" FontSize="11" Foreground="#666666"/>
                <TextBox x:Name="HexInput" Text="#FFFFFFFF" FontSize="14" FontWeight="SemiBold" 
                         Padding="4" BorderThickness="0" Background="Transparent"
                         TextChanged="HexInput_TextChanged"/>
            </StackPanel>
        </Grid>

        <!-- RGB Sliders -->
        <StackPanel Grid.Row="1">
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="R" FontWeight="Bold" Foreground="#FF5252"/>
                <Slider x:Name="SliderR" Grid.Column="1" Maximum="255" Margin="8,0" ValueChanged="Slider_ValueChanged"/>
                <TextBlock x:Name="TextR" Grid.Column="2" Text="{Binding ElementName=SliderR, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Right"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="G" FontWeight="Bold" Foreground="#4CAF50"/>
                <Slider x:Name="SliderG" Grid.Column="1" Maximum="255" Margin="8,0" ValueChanged="Slider_ValueChanged"/>
                <TextBlock x:Name="TextG" Grid.Column="2" Text="{Binding ElementName=SliderG, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Right"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="B" FontWeight="Bold" Foreground="#2196F3"/>
                <Slider x:Name="SliderB" Grid.Column="1" Maximum="255" Margin="8,0" ValueChanged="Slider_ValueChanged"/>
                <TextBlock x:Name="TextB" Grid.Column="2" Text="{Binding ElementName=SliderB, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Right"/>
            </Grid>
            
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="A" FontWeight="Bold" Foreground="#9E9E9E"/>
                <Slider x:Name="SliderA" Grid.Column="1" Maximum="255" Margin="8,0" ValueChanged="Slider_ValueChanged"/>
                <TextBlock x:Name="TextA" Grid.Column="2" Text="{Binding ElementName=SliderA, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>

        <!-- Presets -->
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
             <TextBlock Text="预设颜色" Margin="0,0,0,8" Foreground="#666666" FontSize="12"/>
             <WrapPanel x:Name="PresetPanel">
                 <!-- Populated in Code Behind -->
             </WrapPanel>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="取 消" Width="80" Click="Cancel_Click"/>
            <Button Grid.Column="3" Content="确 定" Width="80" Style="{StaticResource AccentButton}" Click="Confirm_Click"/>
        </Grid>
    </Grid>
</Window>
